services:
  trainwatch-backend:
    build:
      context: ./backend
      target: dev
    volumes:
      - ./backend:/app
    env_file:
      - ./backend/.env

  traefik:
    image: traefik:v3.2
    ports:
      - "443:443"
      - "8081:8080"
    volumes:
      - ./docker/traefik.yml:/etc/traefik/traefik.yml:ro
      - ./docker/services.yml:/etc/traefik/services.yml:ro
      - ./docker/certs:/etc/traefik/certs:ro
