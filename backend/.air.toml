root = "."
tmp_dir = ".tmp"

[build]
  cmd = "go build -o .tmp/main ./cmd/main.go"
  bin = ".tmp/main"
  delay = 1000
  exclude_dir = ["vendor", ".tmp"]
  include_ext = ["go", "env"]
  kill_delay = "0s"
  rerun_delay = 500

[log]
  time = false

[misc]
  clean_on_exit = true
